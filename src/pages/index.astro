<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="idr.to is building ID-Based Routing for the next generation of connected devices."
    />
    <title>idr.to — Coming Soon</title>
  </head>
  <body>
    <main>
      <p class="wordmark">idr.to</p>
      <p class="eyebrow">ID-Based Routing</p>
      <h1>Coming soon.</h1>
      <p class="intro">
        A simpler way to reach the devices that matter, wherever they are.
      </p>
    </main>
  </body>
</html>

<style>
  :global(*) {
    box-sizing: border-box;
  }

  :global(body) {
    min-height: 100vh;
    margin: 0;
    display: grid;
    place-items: center;
    background: #0b1020;
    color: #f5f7fb;
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  main {
    width: min(100% - 3rem, 42rem);
    padding: 3rem 0;
  }

  .wordmark {
    margin: 0 0 5rem;
    font-size: 1.125rem;
    font-weight: 700;
    letter-spacing: -0.04em;
  }

  .eyebrow {
    margin: 0 0 1rem;
    color: #79e2d4;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  h1 {
    margin: 0;
    font-size: clamp(3.5rem, 12vw, 7rem);
    line-height: 0.95;
    letter-spacing: -0.07em;
  }

  .intro {
    max-width: 28rem;
    margin: 1.5rem 0 0;
    color: #aab4c8;
    font-size: clamp(1rem, 2vw, 1.25rem);
    line-height: 1.55;
  }
</style>
